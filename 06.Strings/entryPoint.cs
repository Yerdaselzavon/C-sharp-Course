class entryPoint
{
    static void Main()
    {
        string username = "admin";

        System.Console.WriteLine(username[0]);
        // [] index operation for find specefic character, index starts from zero

        // Strings are immutable, they cannot be changed in such a direct way as username[0]=x

    }
}